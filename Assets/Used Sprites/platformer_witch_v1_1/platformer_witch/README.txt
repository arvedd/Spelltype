Character Pack - Witch : version 1.0
=================================

this asset pack contains .gif files, .png spritesheets, and .aseprite source files for the following.
=================================================================================

-idle animation, 4 frames, loops, 64x64 px
-running animation, 8 frames, loops, 64x64 px
-jumping animation, 4 frames, loops, 64x64 px
-falling animation, 4 frames, loops, 64x64 px
-jump to fall transition animation, 5 frames, doesn't loop, 64x64 px

- earth spell animations
  - ground variant, 9 frames, doesn't loop, 64x64 px
  - in air variant : start up, 6 frames, doesn't loop, 64x64 px
  - in-air variant : fall, 4 frames, loops, 64x64 px
  - in-air variant : land, 5 frames, doesn't loop, 64x64 px
  - spell effect animation, 13 frames, doesn't loop, 80x80 px

- lightning spell animations
  - ground variant, 9 frames, doesn't loop, 64x64 px
  - in-air variant, 9 frames, doesn't loop, 64x64 px
  - ground hit effect, 10 frames, doesn't loop, 64x32 px
  - spell effect animation, 6 frames, loops,  32x64 px

- fire spell animations
  - ground variant, 7 frames, doesn't loop, 64x64 px
  - in-air variant, 8 frames, doesn't loop, 64x64 px
  - spell effect animation : straight, 9 frames, loops, 48x32 px
  - spell effect animation : diagonal, 9 frames, loops, 48x48 px

- ice spell animations
  - ground variant, 8 frames, doesn't loop, 80x80 px
  - in-air variant, 8 frames, doesn't loop, 80x80 px
  - ice spawn effect, 10 frames, doesn't loop, 80x80 px
  - icicle image, 1 frame, static image, 64x64 px

- special attack animation
  - special start, 16 frames, doesn't loop (leads into next),64x64 px
  - special loop, 4 frames, loops, 64x64 px
  - spell circle effect, 14 frames, doesn't loop, 160x160 px
  - spell glyph effect, 5 frames, loops, 16x16 px	
  - spell projectile effect, 9 frames, loops, 32x32 px
  - spell projectile trail, 9 frames, loops, 32x32 px

----

(these next 3 assume a ladder 32px wide with 16px tall segments)

-ladder climb animation, 8 frames, loops, 64x64 px
-ladder slide animation, 4 frames, loops, 64x64 px
-ladder top animation, 13 frames, doesn't loop, 64x64 px

----

- crouch down, 7 frames, doesn't loop, 64x64 px
- stand up, 7 frames, doesn't loop, 64x64 px
- crouch cast, 8 frames, doesn't loop, 64x64 px
- crawl, 6 frames, loops, 64x64 px

- self cast, 12 frames, doesn't loop, 64x64 px
- shot/cast, 8 frames, doesn't loop, 64x64 px
- win, 16 frames, doesn't loop, 64x64 px


---- 
BAT
----

- bat; transform into, 14 frames, doesn't loop, 64x64 px
- bat; transform from, 10 frames, doesn't loop, 64x64 px
- bat; idle, 7 frames, loops, 64x64 px

----

-hit(standing) animation, 3 frames, doesn't loop. 64x64 px
-hit(thrown/air) animation, 3 frames, doesn't loop. 64x64 px

-toss item animation, 6 frames, doesn't loop 64x64 px
-use item animation, 5 frames, doesn't loop. 64x64 px
-death animation, 15 frames, doesn't loop, 96x64 px

=================================================================================

it also contains .png files and the .clip (Clip Studio Paint) source file for the following.
- a full resolution illustration of the Witch character (4961x7016 px) 
- a quarter sized image of the illustration (more reasonably sized, I work large and downscale) (1240x1754 px)

-for further questions, either hit me up on whatever marketplace you found these on, or shoot me an email at phi9009@gmail.com
